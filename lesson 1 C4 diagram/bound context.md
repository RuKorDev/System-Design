# Bound context
    1.Customer Context (клиент);
    2.Wallet Context (кошельки/счета, баланс, блокировки);
    3.Payments initiator Context (Инициация платежа);
    4.Anti-Fraud Context (Проверка на антифрод);
    5.Payment Executor Context (Исполнитель платежа(выбор ПС))
    6.Payment Hisory Context (История платежей/статистика)
    7.Core Banking Context (Legacy);
    8.Notifications Context (Оповещения);